"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Text = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const constants_1 = require("../../Utils/constants");
function Text(props) {
    const onChange = (e) => {
        if (props.onChange) {
            props.onChange(e);
        }
    };
    const onFocus = (e) => {
        if (props.onFocus) {
            props.onFocus(e);
        }
    };
    const onBlur = (e) => {
        if (props.onBlur) {
            props.onBlur(e);
        }
    };
    const onKeyUp = (e) => {
        if (props.onKeyUp) {
            props.onKeyUp(e);
        }
    };
    return ((0, jsx_runtime_1.jsx)("input", { type: constants_1.INPUT.TEXT, id: props.id, name: props.name, value: props.value, defaultValue: props.defaultValue, pattern: props.pattern, minLength: props.minLength, maxLength: props.maxLength, onChange: onChange, onFocus: onFocus, onBlur: onBlur, onKeyUp: onKeyUp, className: props.className, required: props.required, disabled: props.disabled, placeholder: props.placeholder }));
}
exports.Text = Text;
//# sourceMappingURL=Text.js.map