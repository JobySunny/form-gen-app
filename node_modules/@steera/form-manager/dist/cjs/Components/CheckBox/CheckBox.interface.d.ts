import { LabelProps } from '../Label';
import { HintProps } from '../Hint';
import { CheckProps } from '../Check';
export interface CheckBoxProps extends CheckProps, Omit<LabelProps, 'for' | 'text' | 'className' | 'required'>, Omit<HintProps, 'className' | 'show' | 'message'> {
    /**
     * Label for input
     */
    label: string;
    /**
     * message for invalid inputs
     */
    invalidInputMessage: string;
    /**
     * boolean to set if form submitted
     */
    isSubmitted?: boolean;
}
//# sourceMappingURL=CheckBox.interface.d.ts.map