"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Options = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const Empty_1 = require("./Empty");
const Option_1 = require("./Option");
const Loading_1 = require("./Loading");
/**
 * Options
 *  Component that decides between what to display
 *   - EmptyMessage
 *   - LoadingMessage
 *   - List<Options>
 */
function Options(props) {
    const { show, options = [], testId, loading = false, optionClass = '', className = '', } = props;
    const renderOption = (_a) => {
        var { id, label } = _a, rest = __rest(_a, ["id", "label"]);
        return ((0, jsx_runtime_1.jsx)(Option_1.Option, Object.assign({ id: id, label: label, testId: testId, className: optionClass, onSelect: props.onSelect }, rest), rest.value));
    };
    const renderOptions = () => {
        if (loading) {
            return (0, jsx_runtime_1.jsx)(Loading_1.Loading, { testId: testId });
        }
        if (options.length) {
            return props.options.map(renderOption);
        }
        else {
            return (0, jsx_runtime_1.jsx)(Empty_1.Empty, { testId: testId });
        }
    };
    const optionsTestId = `${testId}_options`;
    if (show) {
        return ((0, jsx_runtime_1.jsx)("ul", Object.assign({ className: className, "data-testid": optionsTestId }, { children: renderOptions() })));
    }
    else {
        return null;
    }
}
exports.Options = Options;
//# sourceMappingURL=Options.js.map