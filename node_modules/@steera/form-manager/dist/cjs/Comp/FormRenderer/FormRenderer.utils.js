"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.setCusotmElementValidity = exports.FormContext = void 0;
const react_1 = __importDefault(require("react"));
exports.FormContext = react_1.default.createContext({
    submitted: false,
});
function isInputValid(element) {
    const { badInput, patternMismatch, rangeOverflow, rangeUnderflow, stepMismatch, tooLong, tooShort, typeMismatch, valueMissing, } = element.validity;
    return ![
        badInput,
        patternMismatch,
        rangeOverflow,
        rangeUnderflow,
        stepMismatch,
        tooLong,
        tooShort,
        typeMismatch,
        valueMissing,
    ].some((flag) => flag);
}
function setCusotmElementValidity(elementId, invalidInputMessage) {
    const element = document.getElementById(elementId);
    if (element) {
        const valid = isInputValid(element);
        if (valid) {
            element.setCustomValidity('');
        }
        else {
            element.setCustomValidity(invalidInputMessage);
        }
    }
}
exports.setCusotmElementValidity = setCusotmElementValidity;
//# sourceMappingURL=FormRenderer.utils.js.map