"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updatePathWithComponent = void 0;
const pathUtils_1 = require("../../../../Utils/pathUtils");
/**
 * Accepts a path and adds the pathkey component as the last component in path.
 * @param path Current Path with its last node as xx/{index}-addnew
 * @param pathKey Component Identifier
 * @returns updatedPath ComponentPath
 */
function updatePathWithComponent(path, pathKey) {
    const pathNodes = pathUtils_1.Path.parseToNodes(path);
    const edgePathNode = pathNodes[pathNodes.length - 1];
    const [index] = pathUtils_1.Path.parseToIndex(edgePathNode);
    const newPathNode = pathUtils_1.Path.makePathNode(pathKey, index);
    const newPathPrefix = pathUtils_1.Path.mergePathNodes(pathNodes.splice(0, pathNodes.length - 1));
    return pathUtils_1.Path.addNodeToPath(newPathPrefix, newPathNode);
}
exports.updatePathWithComponent = updatePathWithComponent;
//# sourceMappingURL=UpdatePathWithComponent.js.map