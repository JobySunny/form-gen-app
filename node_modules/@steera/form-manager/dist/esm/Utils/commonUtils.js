export var KEY;
(function (KEY) {
    KEY["ENTER"] = "Enter";
})(KEY || (KEY = {}));
export function getOnKey(key) {
    return (callback, eve) => {
        if (eve.key === key) {
            callback(eve);
        }
    };
}
const throttleKeys = {};
export function getThrottle(key) {
    return (callback, interval = 500) => {
        if (throttleKeys.hasOwnProperty(key)) {
            clearTimeout(throttleKeys[key]);
        }
        throttleKeys[key] = setTimeout(callback, interval);
    };
}
//# sourceMappingURL=commonUtils.js.map