import { jsx as _jsx } from "react/jsx-runtime";
import { ComponentPath } from '../../Comp.interface';
import { Box, EmailBox, PasswordBox, TextBox, NumberBox, DateBox, RadioBox, CheckBox, SelectBox, Fieldset, Title, } from '../RenderComponents';
export function FormCanvas(props) {
    const renderBox = (c) => (_jsx(Box, Object.assign({ options: c.options }, { children: c.children.map(renderTemplateComponent) }), c.path));
    const renderEmailBox = (c) => (_jsx(EmailBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderTextBox = (c) => (_jsx(TextBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderPasswordBox = (c) => (_jsx(PasswordBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderNumberBox = (c) => (_jsx(NumberBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderDateBox = (c) => (_jsx(DateBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderRadioBox = (c) => (_jsx(RadioBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderCheckBox = (c) => (_jsx(CheckBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderSelectBox = (c) => (_jsx(SelectBox, { options: c.options, templateValues: props.templateValues }, c.path));
    const renderFieldset = (c) => (_jsx(Fieldset, Object.assign({ options: c.options }, { children: c.children.map(renderTemplateComponent) }), c.path));
    const renderTitle = (c) => (_jsx(Title, { options: c.options }, c.path));
    const renderTemplateComponent = (c) => {
        switch (c.pathId) {
            case ComponentPath.box:
                return renderBox(c);
            case ComponentPath.emailbox:
                return renderEmailBox(c);
            case ComponentPath.textbox:
                return renderTextBox(c);
            case ComponentPath.passwordbox:
                return renderPasswordBox(c);
            case ComponentPath.numberbox:
                return renderNumberBox(c);
            case ComponentPath.datebox:
                return renderDateBox(c);
            case ComponentPath.radiobox:
                return renderRadioBox(c);
            case ComponentPath.checkbox:
                return renderCheckBox(c);
            case ComponentPath.selectbox:
                return renderSelectBox(c);
            case ComponentPath.fieldset:
                return renderFieldset(c);
            case ComponentPath.title:
                return renderTitle(c);
            default:
                return null;
        }
    };
    return props.template.map(renderTemplateComponent);
}
//# sourceMappingURL=FormCanvas.js.map