import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { DateBox as DB, } from '../../../../Components/DateBox';
import { FormContext, setCusotmElementValidity } from '../../FormRenderer.utils';
export function DateBox(props) {
    const { submitted } = React.useContext(FormContext);
    const [value, setValue] = React.useState();
    React.useEffect(() => {
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    }, [submitted]);
    React.useEffect(() => {
        setValue(props.value);
    }, [props.value]);
    const onChange = (e) => {
        setValue(e.target.value);
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    };
    return (_jsx(DB, { id: props.id, label: props.label, name: props.name, value: value, className: props.className, required: props.required, min: props.min, max: props.max, invalidInputMessage: props.invalidInputMessage, disabled: props.disabled, onChange: onChange }));
}
//# sourceMappingURL=DateBox.js.map