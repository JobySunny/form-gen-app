import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { RadioBox as RB, } from '../../../../Components/RadioBox';
import { FormContext, setCusotmElementValidity } from '../../FormRenderer.utils';
export function RadioBox(props) {
    const { submitted } = React.useContext(FormContext);
    const [checked, setChecked] = React.useState(props.checked);
    React.useEffect(() => {
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    }, [submitted]);
    React.useEffect(() => {
        setChecked(props.checked);
    }, [props.checked]);
    const onChange = (e) => {
        setChecked(e.target.checked);
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    };
    return (_jsx(RB, { id: props.id, label: props.label, name: props.name, className: props.className, required: props.required, invalidInputMessage: props.invalidInputMessage, isSubmitted: props.isSubmitted, disabled: props.disabled, checked: checked, value: props.value, onChange: onChange }));
}
//# sourceMappingURL=RadioBox.js.map