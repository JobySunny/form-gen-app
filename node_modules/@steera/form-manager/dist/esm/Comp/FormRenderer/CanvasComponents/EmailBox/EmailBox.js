import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { EmailBox as EB, } from '../../../../Components/EmailBox';
import { FormContext, setCusotmElementValidity } from '../../FormRenderer.utils';
export function EmailBox(props) {
    const { submitted } = React.useContext(FormContext);
    const [value, setValue] = React.useState();
    React.useEffect(() => {
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    }, [submitted]);
    React.useEffect(() => {
        setValue(props.value);
    }, [props.value]);
    const onChange = (e) => {
        setValue(e.target.value);
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    };
    return (_jsx(EB, { id: props.id, label: props.label, name: props.name, value: value, pattern: props.pattern, className: props.className, required: props.required, minLength: props.minLength, maxLength: props.maxLength, disabled: props.disabled, placeholder: props.placeholder, invalidInputMessage: props.invalidInputMessage, onChange: onChange }));
}
//# sourceMappingURL=EmailBox.js.map