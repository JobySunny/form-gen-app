import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { PasswordBox as PB, } from '../../../../Components/PasswordBox';
import { FormContext, setCusotmElementValidity } from '../../FormRenderer.utils';
export function PasswordBox(props) {
    const { submitted } = React.useContext(FormContext);
    const [value, setValue] = React.useState();
    React.useEffect(() => {
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    }, [submitted]);
    React.useEffect(() => {
        setValue(props.value);
    }, [props.value]);
    const onChange = (e) => {
        setValue(e.target.value);
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    };
    return (_jsx(PB, { id: props.id, label: props.label, name: props.name, value: value, onChange: onChange, pattern: props.pattern, className: props.className, required: props.required, minLength: props.minLength, maxLength: props.maxLength, invalidInputMessage: props.invalidInputMessage, isSubmitted: props.isSubmitted, disabled: props.disabled, placeholder: props.placeholder }));
}
//# sourceMappingURL=PasswordBox.js.map