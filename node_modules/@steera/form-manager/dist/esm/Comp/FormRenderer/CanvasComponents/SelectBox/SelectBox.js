import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { SelectBox as SB, } from '../../../../Components/SelectBox';
import { FormContext, setCusotmElementValidity } from '../../FormRenderer.utils';
export function SelectBox(props) {
    const { submitted } = React.useContext(FormContext);
    const [value, setValue] = React.useState();
    React.useEffect(() => {
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    }, [submitted]);
    React.useEffect(() => {
        if (props.value)
            setValue(props.value);
    }, [props.value]);
    const onChange = (e) => {
        setValue(e.value);
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    };
    return (_jsx(SB, { id: props.id, value: value, name: props.name, label: props.label, options: props.options, required: props.required, disabled: props.disabled, className: props.className, placeholder: props.placeholder, invalidInputMessage: props.invalidInputMessage, onChange: onChange }));
}
//# sourceMappingURL=SelectBox.js.map