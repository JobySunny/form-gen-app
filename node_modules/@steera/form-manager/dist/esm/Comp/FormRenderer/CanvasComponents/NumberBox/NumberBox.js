import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { NumberBox as NB, } from '../../../../Components/NumberBox';
import { FormContext, setCusotmElementValidity } from '../../FormRenderer.utils';
export function NumberBox(props) {
    const { submitted } = React.useContext(FormContext);
    const [value, setValue] = React.useState();
    React.useEffect(() => {
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    }, [submitted]);
    React.useEffect(() => {
        setValue(props.value);
    }, [props.value]);
    const onChange = (e) => {
        setValue(e.target.value);
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    };
    return (_jsx(NB, { id: props.id, label: props.label, name: props.name, value: value, className: props.className, required: props.required, min: props.min, max: props.max, invalidInputMessage: props.invalidInputMessage, disabled: props.disabled, placeholder: props.placeholder, onChange: onChange }));
}
//# sourceMappingURL=NumberBox.js.map