import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { CheckBox as CB, } from '../../../../Components/CheckBox';
import { FormContext, setCusotmElementValidity } from '../../FormRenderer.utils';
export function CheckBox(props) {
    const { submitted } = React.useContext(FormContext);
    const [checked, setChecked] = React.useState(props.checked);
    React.useEffect(() => {
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    }, [submitted]);
    React.useEffect(() => {
        setChecked(props.checked);
    }, [props.checked]);
    const onChange = (e) => {
        setChecked(e.target.checked);
        if (submitted) {
            setCusotmElementValidity(props.id, props.invalidInputMessage);
        }
    };
    return (_jsx(CB, { id: props.id, label: props.label, name: props.name, className: props.className, required: props.required, value: props.value, invalidInputMessage: props.invalidInputMessage, isSubmitted: props.isSubmitted, disabled: props.disabled, checked: checked, onChange: onChange }));
}
//# sourceMappingURL=CheckBox.js.map