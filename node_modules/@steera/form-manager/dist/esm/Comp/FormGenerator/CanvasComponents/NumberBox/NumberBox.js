import { jsx as _jsx } from "react/jsx-runtime";
import { NumberBox as NB } from '../../../../Components/NumberBox';
import { useAppDispatch, useAppSelector } from '../../StoreProvider';
import { Action, Selector } from '../../Store';
import cn from './NumberBox.module.scss';
export function NumberBox(props) {
    const dispatch = useAppDispatch();
    const isPreview = useAppSelector(Selector.getIsPreview);
    const selectedPath = useAppSelector(Selector.getSelectedNodePath);
    const className = `${props.className} ${cn.viewOnly}`;
    const onClick = (e) => {
        e.stopPropagation();
        dispatch(Action.selectComponent(props.path));
    };
    const wrapClass = `${cn.opt} ${props.path === selectedPath ? cn.selected : ''}`;
    const component = (_jsx(NB, { id: props.id, label: props.label, name: props.name, className: className, required: props.required, min: props.min, max: props.max, invalidInputMessage: props.invalidInputMessage, disabled: props.disabled, placeholder: props.placeholder }));
    if (isPreview) {
        return component;
    }
    else {
        return (_jsx("div", Object.assign({ className: wrapClass, onClick: onClick }, { children: component })));
    }
}
//# sourceMappingURL=NumberBox.js.map