import { jsx as _jsx } from "react/jsx-runtime";
import { PasswordBox as PB } from '../../../../Components/PasswordBox';
import { useAppDispatch, useAppSelector } from '../../StoreProvider';
import { Action, Selector } from '../../Store';
import cn from './PasswordBox.module.scss';
export function PasswordBox(props) {
    const dispatch = useAppDispatch();
    const isPreview = useAppSelector(Selector.getIsPreview);
    const selectedPath = useAppSelector(Selector.getSelectedNodePath);
    const className = `${props.className} ${cn.viewOnly}`;
    const onClick = (e) => {
        e.stopPropagation();
        dispatch(Action.selectComponent(props.path));
    };
    const wrapClass = `${cn.opt} ${props.path === selectedPath ? cn.selected : ''}`;
    const component = (_jsx(PB, { id: props.id, label: props.label, name: props.name, pattern: props.pattern, className: className, required: props.required, minLength: props.minLength, maxLength: props.maxLength, invalidInputMessage: props.invalidInputMessage, isSubmitted: props.isSubmitted, disabled: props.disabled, placeholder: props.placeholder }));
    if (isPreview) {
        return component;
    }
    else {
        return (_jsx("div", Object.assign({ className: wrapClass, onClick: onClick }, { children: component })));
    }
}
//# sourceMappingURL=PasswordBox.js.map