import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { INPUT } from '../../../../Utils/constants';
import { useAppSelector, useAppDispatch } from '../../StoreProvider';
import { Selector, Action } from '../../Store';
export function TemplateName(props) {
    const templateName = useAppSelector(Selector.getTemplateName);
    const dispatch = useAppDispatch();
    const [input, setInput] = React.useState('');
    React.useEffect(() => {
        setInput(templateName);
    }, [templateName]);
    const onChange = (e) => setInput(e.target.value);
    const onBlur = (e) => dispatch(Action.onChangeTemplateName(e.target.value));
    return (_jsx("input", { value: input, required: true, onBlur: onBlur, type: INPUT.TEXT, onChange: onChange, placeholder: 'template name', className: props.className }));
}
//# sourceMappingURL=TemplateName.js.map