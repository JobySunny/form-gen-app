import { jsx as _jsx } from "react/jsx-runtime";
import cn from './Box.module.scss';
export function Box(props) {
    const className = `${cn.box} ${props.className}`;
    const onClick = (e) => {
        e.stopPropagation();
        if (props.onClick) {
            props.onClick();
        }
    };
    return (_jsx("div", Object.assign({ className: className, style: props.style, onClick: onClick }, { children: props.children })));
}
//# sourceMappingURL=Box.js.map