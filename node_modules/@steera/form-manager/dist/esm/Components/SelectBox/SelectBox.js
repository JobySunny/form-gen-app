import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import cn from './SelectBox.module.scss';
import { Block } from './Block';
import { Label } from '../Label';
import { Hint } from '../Hint';
import { SelectInput as Select } from '../SelectInput';
export function SelectBox(props) {
    const [valid, setValid] = React.useState(true);
    const getClassName = (ele = '') => `${cn[`asb${ele}`]} ${props.className}${ele}`;
    const getHintShow = () => {
        if (props.isSubmitted) {
            return !valid;
        }
        else
            return false;
    };
    return (_jsxs(Block, Object.assign({ className: getClassName('') }, { children: [_jsx(Label, { for: props.id, text: props.label, required: props.required, className: getClassName('_label') }), _jsx(Select, { id: props.id, name: props.name, value: props.value, options: props.options, onChange: props.onChange, disabled: props.disabled, required: props.required, placeholder: props.placeholder, testId: `${props.testId}_select`, className: getClassName('_select') }), _jsx(Hint, { show: getHintShow(), message: props.invalidInputMessage, className: getClassName('_hint') })] })));
}
//# sourceMappingURL=SelectBox.js.map