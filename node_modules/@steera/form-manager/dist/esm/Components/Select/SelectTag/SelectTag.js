import { jsx as _jsx } from "react/jsx-runtime";
/**
 * SelectTag
 *  Input Select Tag to associate with Component
 *  This tag is essentially the storage that holds the input and is generally used
 *  fire input events and stuff
 */
export function SelectTag(props) {
    const testId = `${props.testId}_select`;
    const option = (opt) => (_jsx("option", Object.assign({ value: opt.value }, { children: opt.label }), opt.id));
    const { options = [] } = props;
    const onChange = () => { };
    return (_jsx("select", Object.assign({ "aria-readonly": true, id: props.id, name: props.name, value: props.value, "data-testid": testId, onChange: onChange, required: props.required, disabled: props.disabled, className: props.className }, { children: options.map(option) })));
}
//# sourceMappingURL=SelectTag.js.map