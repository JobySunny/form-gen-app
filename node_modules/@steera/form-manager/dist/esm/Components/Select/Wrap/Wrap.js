import { jsx as _jsx } from "react/jsx-runtime";
import { SELECT_TAG } from '../Select.constants';
import { getCheckIfChild } from '../Select.utils';
import cn from '../Select.module.scss';
/**
 * Wrap
 *  Wrapper Component inside which all atoms for AsyncSelect Component are added
 */
export function Wrap(props) {
    const onFocus = (e) => {
        const target = e.target;
        let selectElement = target.querySelector(SELECT_TAG);
        props.onFocus(selectElement);
    };
    const onBlur = (e) => {
        const checkIfChild = getCheckIfChild(e.currentTarget);
        if (!checkIfChild(e.relatedTarget)) {
            const target = e.target;
            let selectElement = target.querySelector(SELECT_TAG);
            props.onBlur(selectElement);
        }
    };
    const className = `${props.className} ${props.disabled ? `${cn.disabled} disabled` : ''}`;
    return (_jsx("div", Object.assign({ tabIndex: 0, onBlur: onBlur, onFocus: onFocus, "data-testid": props.testId, className: className }, { children: props.children })));
}
//# sourceMappingURL=Wrap.js.map