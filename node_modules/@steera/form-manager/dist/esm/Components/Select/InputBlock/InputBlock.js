import { jsx as _jsx } from "react/jsx-runtime";
import { Input } from './Input';
import { InputAlt } from './InputAlt';
/**
 * InputBlock
 *  Component to handle rendering of Selected Option and Typing in Component
 */
export function InputBlock(props) {
    if (props.focused) {
        return (_jsx(Input, { value: props.value, onSearch: props.onSearch, className: props.className, testId: `${props.testId}_input` }));
    }
    else {
        return (_jsx(InputAlt, { label: props.value, className: props.className, placeholder: props.placeholder, testId: `${props.testId}_inputalt` }));
    }
}
//# sourceMappingURL=InputBlock.js.map