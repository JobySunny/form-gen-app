import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { INPUT_TYPE_TEXT, INITIAL_SEARCH_TEXT, } from '../../Select.constants';
import { getThrottle } from '../../Select.utils';
/**
 * Input
 *  HTML Input element; Fires onSearch with throttle;
 */
export function Input(props) {
    var _a;
    const throttle = getThrottle('onSearch');
    const [search, setSearch] = React.useState((_a = props.value) !== null && _a !== void 0 ? _a : INITIAL_SEARCH_TEXT);
    const onChange = (e) => {
        setSearch(e.target.value);
    };
    React.useEffect(() => {
        if (!(search === props.value)) {
            throttle(() => {
                props.onSearch(search);
            });
        }
    }, [search]);
    return (_jsx("input", { tabIndex: -1, value: search, autoFocus: true, onChange: onChange, type: INPUT_TYPE_TEXT, "data-testid": props.testId, className: props.className }));
}
//# sourceMappingURL=Input.js.map