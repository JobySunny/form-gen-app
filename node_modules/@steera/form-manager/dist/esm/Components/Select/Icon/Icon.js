import { jsx as _jsx } from "react/jsx-runtime";
import { Focused } from './Focused';
import { Normal } from './Normal';
import { Loading } from './Loading';
export function Icon(props) {
    const renderContent = () => {
        if (props.loading) {
            return _jsx(Loading, { testId: props.testId, className: props.iconClassName });
        }
        if (props.focus) {
            return _jsx(Focused, { testId: props.testId, className: props.iconClassName });
        }
        else {
            return _jsx(Normal, { testId: props.testId, className: props.iconClassName });
        }
    };
    return _jsx("div", Object.assign({ className: props.className }, { children: renderContent() }));
}
//# sourceMappingURL=Icon.js.map