var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import { getOnKey, KEY } from '../../../../Utils/commonUtils';
export function Option(props) {
    const onEnterKey = getOnKey(KEY.ENTER);
    const { onSelect, className, testId } = props, rest = __rest(props, ["onSelect", "className", "testId"]);
    const onClick = () => {
        onSelect(rest);
    };
    const onKeyUp = (eve) => {
        onEnterKey(() => {
            onSelect(rest);
        }, eve);
    };
    const dataTestId = `${testId}_options__item`;
    return (_jsx("li", Object.assign({ tabIndex: 0, onClick: onClick, onKeyUp: onKeyUp, "data-testid": dataTestId, className: className }, { children: rest.label })));
}
//# sourceMappingURL=Option.js.map