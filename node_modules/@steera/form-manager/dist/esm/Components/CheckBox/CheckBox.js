import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import React from 'react';
import { Check } from '../Check';
import { Label } from '../Label';
import { Hint } from '../Hint';
import cn from './CheckBox.module.scss';
export function CheckBox(props) {
    const [valid, setValid] = React.useState(true);
    const getClassName = (ele = '') => `${cn[`cbox${ele}`]} ${props.className}${ele}`;
    const getHintShow = () => {
        if (props.isSubmitted) {
            return !valid;
        }
        else
            return false;
    };
    return (_jsxs("div", Object.assign({ className: getClassName() }, { children: [_jsx(Check, { id: props.id, name: props.name, value: props.value, className: getClassName('_input'), disabled: props.disabled, required: props.required, onChange: props.onChange, checked: props.checked }), _jsx(Label, { for: props.id, text: props.label, required: props.required, className: getClassName('_label') }), _jsx(Hint, { show: getHintShow(), message: props.invalidInputMessage, className: getClassName('_hint') })] })));
}
//# sourceMappingURL=CheckBox.js.map