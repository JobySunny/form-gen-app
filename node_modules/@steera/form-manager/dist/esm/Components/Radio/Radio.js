import { jsx as _jsx } from "react/jsx-runtime";
import { INPUT } from '../../Utils/constants';
export function Radio(props) {
    const onChange = (e) => {
        if (props.onChange) {
            props.onChange(e);
        }
    };
    return (_jsx("input", { type: INPUT.RADIO, id: props.id, name: props.name, value: props.value, onChange: onChange, checked: props.checked, className: props.className, required: props.required, disabled: props.disabled }));
}
//# sourceMappingURL=Radio.js.map