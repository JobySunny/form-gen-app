import { jsx as _jsx } from "react/jsx-runtime";
import { INPUT } from '../../Utils/constants';
export function Check(props) {
    const onChange = (e) => {
        if (props.onChange) {
            props.onChange(e);
        }
    };
    return (_jsx("input", { id: props.id, name: props.name, value: props.value, checked: props.checked, onChange: onChange, className: props.className, required: props.required, type: INPUT.CHECKBOX, disabled: props.disabled }));
}
//# sourceMappingURL=Check.js.map