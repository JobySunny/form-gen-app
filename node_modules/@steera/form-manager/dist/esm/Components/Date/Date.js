import { jsx as _jsx } from "react/jsx-runtime";
import { INPUT } from '../../Utils/constants';
export function Date(props) {
    const onChange = (e) => {
        if (props.onChange) {
            props.onChange(e);
        }
    };
    const onFocus = (e) => {
        if (props.onFocus) {
            props.onFocus(e);
        }
    };
    const onBlur = (e) => {
        if (props.onBlur) {
            props.onBlur(e);
        }
    };
    const onKeyUp = (e) => {
        if (props.onKeyUp) {
            props.onKeyUp(e);
        }
    };
    return (_jsx("input", { type: INPUT.DATE, id: props.id, name: props.name, value: props.value, defaultValue: props.defaultValue, min: props.min, max: props.max, onChange: onChange, onFocus: onFocus, onBlur: onBlur, onKeyUp: onKeyUp, className: props.className, required: props.required, disabled: props.disabled }));
}
//# sourceMappingURL=Date.js.map