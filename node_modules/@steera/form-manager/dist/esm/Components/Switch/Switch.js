import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Input } from './Input';
import { Knob } from './Knob';
import { Wrap } from './Wrap';
import cn from './Switch.module.scss';
export function Switch(props) {
    const onChange = (e) => {
        props.onChange({
            name: e.target.name,
            value: e.target.value,
            checked: e.target.checked,
        }, e);
    };
    const wrapClassName = `${cn.sw} ${props.checked ? cn.swOn : cn.swOff} ${props.className}`;
    const inputClassName = `${cn.sw_input} ${props.className}-input`;
    const knobClassName = `${cn.sw_knob} ${props.className}-knob`;
    return (_jsxs(Wrap, Object.assign({ className: wrapClassName }, { children: [_jsx(Input, { id: props.id, name: props.name, value: props.value, onChange: onChange, checked: props.checked, className: inputClassName }), _jsx(Knob, { className: knobClassName })] })));
}
//# sourceMappingURL=Switch.js.map