export interface LabelProps {
  /**
   * for attribute
   */
  for: string;
  /**
   * text for label
   */
  text: string;
  /**
   * className for label
   */
  className: string;
  /**
   * boolean to set label required
   */
  required: boolean;
}
