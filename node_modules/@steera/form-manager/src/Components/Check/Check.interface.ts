import React from 'react';

export interface CheckProps {
  /**
   * name  of input
   */
  name: string;
  /**
   * value of input
   */
  value: string;
  /**
   * Id for input
   */
  id: string;

  /**
   * ClassName for input
   */
  className: string;
  /**
   * required for input
   */
  required: boolean;
  /**
   * disabled for input
   */
  disabled: boolean;

  /**
   * onChange event Handler
   */
  onChange?: React.ChangeEventHandler;

  /**
   * boolean set if checkbox is checked
   */
  checked: boolean;
  /**
   * to Handle when clicked on Enter key
   */

  onEnterKey?: React.KeyboardEventHandler;
}
