import {ComponentPath} from '../../../Comp.interface';
import {EmailBoxProps as EProps} from '../../../../Components/EmailBox';

export interface EmailBoxProps extends Omit<EProps, 'isSubmitted'> {
  /**
   * Unique path to component
   */
  path: string;
  /**
   * pathId to component
   */
  pathId: ComponentPath;
}
