import {ComponentPath} from '../../../Comp.interface';
import {RadioBoxProps as RProps} from '../../../../Components/RadioBox';

export interface RadioBoxProps extends RProps {
  /**
   * Unique path to component
   */
  path: string;
  /**
   * pathId to component
   */
  pathId: ComponentPath;
}
