import {ComponentPath} from '../../../Comp.interface';
import {TitleProps as TProps} from '../../../../Components/Title';

export interface TitleProps extends Omit<TProps, 'isSubmitted'> {
  /**
   * Unique path to component
   */
  path: string;
  /**
   * pathId to component
   */
  pathId: ComponentPath;
}
