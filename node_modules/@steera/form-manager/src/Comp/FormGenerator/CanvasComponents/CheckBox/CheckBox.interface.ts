import {ComponentPath} from '../../../Comp.interface';
import {CheckBoxProps as Cprops} from '../../../../Components/CheckBox';

export interface CheckBoxProps extends Cprops {
  /**
   * Unique path to component
   */
  path: string;
  /**
   * pathId to component
   */
  pathId: ComponentPath;
}
