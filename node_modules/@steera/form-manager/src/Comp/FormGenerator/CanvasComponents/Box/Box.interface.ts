import {ComponentPath} from '../../../Comp.interface';
import {BoxProps as BProps} from '../../../../Components/Box';

export interface BoxProps extends BProps {
  /**
   * Unique path to component
   */
  path: string;
  /**
   * pathId to component
   */
  pathId: ComponentPath;
}
