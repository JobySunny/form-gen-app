import {SelectBoxProps as EProps} from '../../../../Components/SelectBox';
import {ComponentPath} from '../../../Comp.interface';

export interface SelectBoxProps extends Omit<EProps, 'isSubmitted'> {
  /**
   * Unique path to component
   */
  path: string;
  /**
   * pathId to component
   */
  pathId: ComponentPath;
}
